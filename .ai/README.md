# `.ai/`

Reserved for assets meant to be read by AI coding agents working in this repository.

It is empty by default: the root `.gitignore` denies its contents, and anything published
here is opted in explicitly. The contributor guide itself is `AGENTS.md` at the repository
root.
