---
"elepha": minor
---

OpenCode top-level chats now receive and manage chat-scoped standing rules. The plugin verifies each native session with OpenCode's own session lookup; subagent sessions, mismatched or failed lookups keep project rules but get no chat-rule access.
