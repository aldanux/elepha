---
"elepha": minor
---

Index existing history when enabling "Memory-Plus" and automatically refresh missing or stale vectors every minute in an isolated daemon worker. Keep live capture responsive during model loading and indexing, retain completed vectors after failures, and remove the manual indexing step from normal setup.
