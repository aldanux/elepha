---
"elepha": minor
---

Add `elepha disable memory-plus` to turn off semantic indexing and search without deleting stored vectors. Add `elepha uninstall memory-plus` to preview and confirm removal of the isolated local runtime, turn the feature off, and retain vectors and downloaded models. Re-enabling reuses current vectors.
