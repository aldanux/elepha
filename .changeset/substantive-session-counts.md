---
"elepha": patch
---

Count sessions consistently: `elepha projects` now reports the same substantive (searchable and listable) session count as `elepha:info`, `elepha:list`, and `elepha:query`, instead of a raw row count.
