---
"elepha": patch
---

Load the generated OpenCode plugin in V2 while preserving V1 hooks. Apply standing rules to each V2 model request using the session's project directory, and render in-chat commands only in the model-facing context.
