---
"elepha": minor
---

Use opt-in "Memory-Plus" vectors in explicit recall and elepha:query searches. Rank compatible vectors by cosine similarity, then append lexical-only sessions without duplicates. Label semantic provenance, revalidate consent and vector eligibility at read time, and keep provider creation and vector scans disabled when "Memory-Plus" is off.
