---
"elepha": patch
---

Refine the `elepha:info` status line for readability: labelled `capture:` and `sessions:` segments, a constant `X here / Y total` count, and `last session in project: <name> - <age> ago in <tool surface>`.
